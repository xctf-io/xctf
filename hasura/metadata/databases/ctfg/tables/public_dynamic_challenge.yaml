table:
  name: dynamic_challenge
  schema: public
object_relationships:
  - name: challenge
    using:
      foreign_key_constraint_on: id
select_permissions:
  - role: user
    permission:
      columns:
        - decay
        - id
        - initial
        - minimum
      filter:
        challenge:
          state:
            _eq: visible
