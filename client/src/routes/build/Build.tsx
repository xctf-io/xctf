import React from 'react'
import {Graph} from "@/routes/build/Graph";

export const Build: React.FC = () => {
    return (
        <div>
            <h1>Build Page</h1>
            <Graph />
        </div>
    )
}